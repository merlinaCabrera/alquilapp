<%= stylesheet_link_tag 'style.css' %>

<div class="fondo">
    <%= render "/home/menu_lateral"%>

    <div class="encabezado">
        <h1 class="titulo_seccion"> Supervisores </h1>
        <%= link_to crear_super_path do %>
            <%= image_tag "plus.svg", class:"ag_super" %>    
        <% end %>
    </div>
    

        <% if User.where(super: true).count == 0 %>
            <center>
            <h2>Aún no hay supervisores registrados.</h2>
            </center>
        <% else %>
            <center>
            <% User.all.each do |user| %>
                <% if user.super %>
                        <%= link_to supervisor_path( user ) do %>
                            <h2><FONT COLOR="e7c078"><%= user.nombreUsuario %><FONT></h2>
                        <% end %>  
                <% end %>
            <% end %>
            </center>
        <% end %>
        </ul>

    </table>
</div>